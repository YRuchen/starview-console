apiVersion: v1
kind: Service
metadata:
  name: {{ include "obs-frontend.fullname" . }}
  labels:
    {{- include "obs-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "obs-frontend.selectorLabels" . | nindent 4 }}
