# 部署步骤

## 1. 更改版本

values 目录下文件对应环境说明
* values-dev.yaml : eks dev 环境
* values-prod.yaml : eks prod 环境

### 1.1) 修改对应环境values中的镜像tag, 修改为目标镜像tag

```yaml
image:
  repository: "harbor.gainetics.io/observable/images/obs-frontend"
  pullPolicy: IfNotPresent
  tag: "1.0.30-prod"
```

### 1.2) 升级chart

对 `chart.yaml` 文件以下内容进行调整

```yaml
version: 1.0.13
appVersion: "1.0.30"
```

`version` 对应chart版本号，保持滚动升级

`appversion` 对应app版本号，可选项

## 2. helm 打包

执行以下打包命令(记得清理之前打的包)
```shell
helm package .
```
执行后，路径下会生成 `obs-frontend-1.0.14.tgz` 类似的包

## 3. 推送helm chart包到harbor

将打好的包推送到harbor。执行推送命令:
```shell
helm push obs-frontend-1.0.14.tgz oci://harbor.gainetics.io/observable/charts
```
> 确保已经登录了harbor。
> docker 登录harbor命令：docker login -u username -p password
> 

推送成功会有如下信息：
```shell
Pushed: harbor.gainetics.io/observable/charts/obs-frontend:1.0.14
Digest: sha256:78d86cde42c8d471e17426a4bf6c11796a8e490f3c1f5931d42d3fc6cafb8b8c
```

可再次到harbor的chart仓库查看

## 4. eks 部署

登录对应环境的跳板机(jumpserver)

### 4.1 登录harbor

登录命令:
```shell
helm registry login http://harbor.gainetics.io -u cd -p 54IH1EqY/UGU4J5FMNqtVAQb
```

### 4.2 拉取chart

拉取命令：
```shell
helm pull oci://harbor.gainetics.io/observable/charts/obs-frontend --version 1.0.14
```
> 通过 version 指定版本

拉取成功可查看到包
```shell
hk-gainetics-eks-scloud-obs-prod-read-write# helm pull oci://harbor.gainetics.io/observable/charts/obs-frontend --version 1.0.14
Pulled: harbor.gainetics.io/observable/charts/obs-frontend:1.0.14
Digest: sha256:78d86cde42c8d471e17426a4bf6c11796a8e490f3c1f5931d42d3fc6cafb8b8c

hk-gainetics-eks-scloud-obs-prod-read-write# ls
obs-frontend-1.0.14.tgz
```

### 4.3 解压

执行命令：
```shell
 tar -xzf obs-frontend-1.0.14.tgz 
```

### 4.4 helm 部署

执行命令：
```shell
# 更新不部署
helm upgrade frontend obs-frontend-1.0.14.tgz --namespace scloud-obs-prod --values ./obs-frontend/values/values-prod.yaml 
```

参数说明：
`frontend` service name
`--namespace` 指定namespace
`--values` 指定部署的配置文件
